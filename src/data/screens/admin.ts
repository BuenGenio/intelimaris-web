import type { ScreenGroup } from './types'
/* Filled in by the screen pass; empty groups are skipped by the registry. */
export const ADMIN: ScreenGroup = { id: 'admin', label: 'admin', kind: 'platform', app: 'WaterWayz', frame: 'phone', slug: 'admin', doors: [{ id: 'home', label: 'Home' }], screens: [] }
