import type { ScreenGroup } from './types'
/* Filled in by the screen pass; empty groups are skipped by the registry. */
export const VESSEL: ScreenGroup = { id: 'vessel', label: 'vessel', kind: 'platform', app: 'WaterWayz', frame: 'phone', slug: 'vessel', doors: [{ id: 'home', label: 'Home' }], screens: [] }
