import type { ScreenGroup } from './types'
/* Filled in by the screen pass; empty groups are skipped by the registry. */
export const YARD: ScreenGroup = { id: 'yard', label: 'yard', kind: 'platform', app: 'WaterWayz', frame: 'phone', slug: 'yard', doors: [{ id: 'home', label: 'Home' }], screens: [] }
