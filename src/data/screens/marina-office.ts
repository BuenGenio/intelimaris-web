import type { ScreenGroup } from './types'
/* Filled in by the screen pass; empty groups are skipped by the registry. */
export const MARINA_OFFICE: ScreenGroup = { id: 'marina-office', label: 'marina-office', kind: 'platform', app: 'WaterWayz', frame: 'phone', slug: 'marina-office', doors: [{ id: 'home', label: 'Home' }], screens: [] }
