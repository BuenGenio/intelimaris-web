import type { ScreenGroup } from './types'
/* Filled in by the screen pass; empty groups are skipped by the registry. */
export const MARINA_OPS: ScreenGroup = { id: 'marina-ops', label: 'marina-ops', kind: 'platform', app: 'WaterWayz', frame: 'phone', slug: 'marina-ops', doors: [{ id: 'home', label: 'Home' }], screens: [] }
