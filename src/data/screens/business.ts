import type { ScreenGroup } from './types'
/* Filled in by the screen pass; empty groups are skipped by the registry. */
export const BUSINESS: ScreenGroup = { id: 'business', label: 'business', kind: 'platform', app: 'WaterWayz', frame: 'phone', slug: 'business', doors: [{ id: 'home', label: 'Home' }], screens: [] }
