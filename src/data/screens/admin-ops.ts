import type { ScreenGroup } from './types'
/* Filled in by the screen pass; empty groups are skipped by the registry. */
export const ADMIN_OPS: ScreenGroup = { id: 'admin-ops', label: 'admin-ops', kind: 'platform', app: 'WaterWayz', frame: 'phone', slug: 'admin-ops', doors: [{ id: 'home', label: 'Home' }], screens: [] }
